# Dependencies
node_modules/

# Environment / secrets
.env

# Local complaints fallback store (used only when MONGODB_URI is not set)
data/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
npm-debug.log*
